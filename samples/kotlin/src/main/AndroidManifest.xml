<?xml version="1.0" encoding="utf-8"?>

<manifest
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  package="co.windly.ktxprefs.sample.kotlin">

  <!-- region Application -->

  <application
    android:name=".application.SampleApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    tools:ignore="AllowBackup,GoogleAppIndexingWarning">

    <!-- region Activities -->

    <activity
      android:name=".presentation.coroutine.CoroutineActivity" />

    <activity
      android:name=".presentation.reactive.ReactiveActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
      android:name=".presentation.simple.SimpleActivity" />

    <!-- endregion -->

  </application>

  <!-- endregion -->

</manifest>
